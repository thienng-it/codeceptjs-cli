# Dry Run (Test Preview)

The `ccjs dry-run` command simulates a test run without actually opening a browser. It parses your features and scenarios to validate syntax and provide a visual step tree.

## Usage

```bash
# Preview tests without executing
ccjs dry-run

# Preview tests with all their steps expanded
ccjs dry-run --steps
```
